# video-player

Video player 是用于播放视频、并支持基于流控算法自动调整视频播放质量的前端。

## Usage

* 首先，需要安装 nodejs 相关的开发工具。
* 而后，在这个目录下通过 `npm install` 安装依赖。
* `npm start` 可以启动前端。
* `npm run build` 可以将前端编译成 HTML + js + css 文件。之后可以和 `runserver.py` 打包发布。
* 在根目录下 `make {format/lint}-nodejs-video-player` 可以格式化/检查代码风格。
